#include <stdio.h>

/**
 * this function prints quote ""with proper grammar, but the outcome is a piece of art" followed by a new line.
 *
 * Return: zero on success
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
